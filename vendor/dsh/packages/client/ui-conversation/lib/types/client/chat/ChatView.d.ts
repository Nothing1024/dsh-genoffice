import type { ChatViewSlotProps } from '../contract/slots.ts';
/**
 * The chat view slot entry: pure component over the composed props (tool rows
 * render through the declared keyed hole's renderSlot share).
 */
export declare function ChatView({ useSession, useSessions, useStore, renderSlot, sessionId, openFile, openDetails, closeDetails, loadOlder, inspectCall, chatScroll, forkAt, t, }: ChatViewSlotProps): import("react").JSX.Element;
//# sourceMappingURL=ChatView.d.ts.map