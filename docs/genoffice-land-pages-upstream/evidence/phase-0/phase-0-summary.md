# Phase 0 summary

契约冻结完成：`pptx_land_pages` / `land_pages` 入表，smoke 计数 pptx 38→39（skill 37→38）。

BR-007：契约文件与 `scripts/dev.mjs` 数字已改。插件 `CONTROL_TOOL_TABLE` 本包未改；实测 `plugin/dsh-genoffice` 已含 `pptx_land_pages`，因此后续 smoke 的 host 表也绿（非本包改动）。
