# dependencies — pnpm-managed, never commit
node_modules/

# test / tool caches
.vite/
coverage/
*.tsbuildinfo

# logs
*.log

# local DSH_HOME (loopback profile). Never commit secrets.
env/.env
env/.credentials.yaml
env/.anonymous-user-id

# Finder
.DS_Store
